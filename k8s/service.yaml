# ==========================================================================
# K8s Service – 婚礼邀请函
# ==========================================================================
apiVersion: v1
kind: Service
metadata:
  name: wedding-invitation
  labels:
    app: wedding-invitation
spec:
  type: ClusterIP
  selector:
    app: wedding-invitation
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
